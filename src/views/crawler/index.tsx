import { WelcomeCard } from '../../components';

const Crawler = () => {
  return (
    <div>
      <WelcomeCard text="A simple crawler game" title='Crawler' />
    </div>
  );
};

export default Crawler;
